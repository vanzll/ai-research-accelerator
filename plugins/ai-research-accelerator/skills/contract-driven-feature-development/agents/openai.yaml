interface:
  display_name: "Contract-Driven Feature Development"
  short_description: "Build large features with fresh implementation and review."
  default_prompt: "Use a concise frozen contract, a fresh implementation agent, and an independent fresh reviewer to deliver this substantial feature without regressing existing behavior."
